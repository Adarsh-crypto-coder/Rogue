package com.example;

import org.junit.jupiter.api.Test;
//import static org.junit.jupiter.api.Assertions.assertEquals;

class AppTest {
    @Test
    void testAppGreeting() {
        Main app = new Main();
        //assertEquals("Hello, World!", app.getGreeting(), "App should return greeting message");
    }
}
